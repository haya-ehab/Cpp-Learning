#include <iostream>

int main() {


    // Nested Loops -> a loop inside another loop
 
    for(int i = 1; i <= 10; i++){
        std::cout << 1 << ' ';

    }



    // Keep the window open
    std::cout << "\nPress Enter to continue...";
    std::cin.ignore();
    std::cin.get();
    return 0;
}

